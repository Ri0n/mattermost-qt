name: Fix refresh local annotations

on:
  push:
    paths:
      - '.github/workflows/fix-refresh-local-annotations.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/post-disk-cache
          fetch-depth: 0
      - name: Apply fix
        run: python3 .github/scripts/fix-refresh-local-annotations.py
      - name: Remove temporary helper
        run: rm .github/scripts/fix-refresh-local-annotations.py .github/workflows/fix-refresh-local-annotations.yml
      - name: Commit result
        run: |
          git config user.name 'Sergei Ilinykh'
          git config user.email 'rion4ik@gmail.com'
          git add -A
          git commit -m 'Preserve local post annotations on refresh'
          git push origin HEAD:chatgpt/post-disk-cache
