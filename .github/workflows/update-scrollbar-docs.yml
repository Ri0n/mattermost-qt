name: Update scrollbar architecture docs

on:
  push:
    paths:
      - '.github/workflows/update-scrollbar-docs.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/post-disk-cache
          fetch-depth: 0
      - name: Update docs
        run: python3 .github/scripts/update-scrollbar-docs.py
      - name: Remove temporary files
        run: rm .github/scripts/update-scrollbar-docs.py .github/workflows/update-scrollbar-docs.yml
      - name: Commit result
        run: |
          git config user.name 'Sergei Ilinykh'
          git config user.email 'rion4ik@gmail.com'
          git add -A
          git commit -m 'Document scrollbar materialization policy'
          git push origin HEAD:chatgpt/post-disk-cache
