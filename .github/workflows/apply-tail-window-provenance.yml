name: Apply tail-window provenance

on:
  push:
    paths:
      - '.github/workflows/apply-tail-window-provenance.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: chatgpt/post-disk-cache
          fetch-depth: 0
      - name: Apply patch
        run: python3 .github/scripts/apply-tail-window-provenance.py
      - name: Remove temporary helper
        run: rm .github/scripts/apply-tail-window-provenance.py .github/workflows/apply-tail-window-provenance.yml
      - name: Commit result
        run: |
          git config user.name 'Sergei Ilinykh'
          git config user.email 'rion4ik@gmail.com'
          git add -A
          git commit -m 'Track cache tail-window provenance'
          git push origin HEAD:chatgpt/post-disk-cache
